1. Around 100 hours to complete the assignment
2. Need more than 1 user to actually use the network
3. Implement a database to simplify the requirements in the 'Node user' section (like an exchange application would) use sqlite -> username, password, private key, public key
    - Check if the user exists
5-10 transactions per block including the transaction which is the reward for mining the block (might be in the next blockt though)
DAT file for non-db items
Serialization using Pickle. Converting back to the original format only has to be done inthe verify functino. For the rest of the application, the serialized verison can be used.


MIning reward and signup reward should be the same

The miner gets both the mining reward + gasfees